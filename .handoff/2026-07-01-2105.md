# Handoff — 2026-07-01 21:05

## Estado

Plugin deep-memory v2.1.0 listo. Commit pushed a `origin/main`.

## Próximo paso (pendiente)

Reiniciar opencode y verificar que:
- `~/.config/opencode/plugins/deep-memory.ts` carga automáticamente
- Se crea `~/.config/opencode/deep-memory.log` con entrada de startup
- Se crea `~/.config/opencode/storage/deep-memory.db`

## Archivos clave

| Archivo | Propósito |
|---|---|
| `src/deep-memory.ts` | Código fuente del plugin |
| `~/.config/opencode/deep-memory.json` | Config persistente |
| `~/.config/opencode/plugins/deep-memory.ts` | Plugin deployado |
| `~/.config/opencode/storage/` | Directorio donde se crea la DB |

## Referencias

- Último commit: `2f08c8f` — "v2.1.0: rename to deep-memory, file-based logging + config"
- Rama: `main`

## Suggested skills

- `diagnose` — si el plugin no carga al reiniciar opencode
