# Handoff — 2026-07-01 21:43

## Estado

Plugin deep-memory v3.0.0 — single-script refactor. Funcional, auto-cargado desde `plugins/`, verificado.

## Cambio aplicado

**Fix `HOME` fallback** (`deep-memory.ts:7`):

```diff
- import { mkdirSync, existsSync, appendFileSync, readFileSync } from "node:fs";
+ import { mkdirSync, existsSync, appendFileSync, readFileSync } from "node:fs";
+ import { homedir } from "node:os";

- const HOME = process.env.HOME || "/tmp";
+ const HOME = process.env.HOME || homedir();
```

**Razón:** `/tmp` no es un HOME válido (riesgo de seguridad + semánticamente roto). `os.homedir()` provee fallback cross-platform correcto (Windows usa `USERPROFILE` internamente).

## Verificación

- `bun build --target=bun` → bundles cleanly (15.61 KB, +1.5 KB por import extra)
- Deploy: `cp deep-memory.ts ~/.config/opencode/plugins/deep-memory.ts` → OK
- `diff -q` source vs deployed → identical

## Pendiente

- Reiniciar opencode para que cargue la nueva versión
- Verificar `~/.config/opencode/deep-memory.log` → nuevo `[INFO]: Initialized` post-reinicio

## Suggested skills

- Ninguno por ahora.

## Referencias

- Rama: `main`
- Sesión opencode previa: `qedzcpj3mk9x` (2026-07-02 00:36:44)
- Plugin loader: opencode auto-loads desde `~/.config/opencode/plugins/*.ts`
