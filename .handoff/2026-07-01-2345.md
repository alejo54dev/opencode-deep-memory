# Handoff — 2026-07-01 23:45

## Estado

Plugin deep-memory v2.1.1 — bugfix en `pruneOldTurns`. Funcional y registrado.

## Lo que se hizo

1. **Diagnóstico**: Confirmado que opencode soporta `bun:sqlite` y Bun runtime en
   plugins (sqlite-test.ts funcionó correctamente, creando hello.db).
2. **Bug encontrado**: `pruneOldTurns` en `src/deep-memory.ts` — la variable `count`
   se destructureaba a número vía `.c` al final de la línea, pero luego se accedía
   como `count.c` (undefined), produciendo NaN en `LIMIT`.
3. **Fix**: Separé la extracción de `row.c` → `count`, usé `count` directamente.
4. **Deploy**: Copiado a `~/.config/opencode/plugins/deep-memory.ts`.
5. **Config**: Agregado `"./plugins/deep-memory.ts"` al array `"plugin"` en
   `~/.config/opencode/opencode.jsonc`.

## Archivos modificados

| Archivo | Cambio |
|---|---|
| `src/deep-memory.ts` | Fix `pruneOldTurns` (líneas 336-342) |
| `.handoff/2026-07-01-2345.md` | Este handoff |

## Archivos externos modificados

| Archivo | Cambio |
|---|---|
| `~/.config/opencode/plugins/deep-memory.ts` | Copia deployada del fix |
| `~/.config/opencode/opencode.jsonc` | Registro del plugin |

## Pendiente

- Verificar que al reiniciar opencode el plugin cargue automáticamente
- Verificar `~/.config/opencode/deep-memory.log` con entrada `[INFO]: Initialized`

## Suggested skills

- `diagnose` — si el plugin no carga al reiniciar opencode

## Referencias

- Rama: `main`
- Último commit: `3a81ec8` — "handoff: save session state after rename"
- Investigación: bun:sqlite funciona en runtime de plugins; el problema del plugin
  era bug de código + no estar registrado en config
